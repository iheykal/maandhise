{
  "name": "maandhise-corporate",
  "version": "1.0.0",
  "description": "SAHAL CARD - Education, Consulting & Sahal Card Platform",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "start": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "maandhise",
    "sahal-card",
    "education",
    "consulting",
    "discount-card"
  ],
  "author": "SAHAL CARD",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "node-cron": "^4.2.1"
  }
}
